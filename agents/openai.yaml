interface:
  display_name: "Video Maker"
  short_description: "B站视频制作主 skill，优先本地 Qwen；真实上传需配合 desktop-control-for-windows"
  default_prompt: "Use $video-maker to scaffold or upgrade a B站 video project with local Qwen-first TTS, local rendering, publish metadata generation, and desktop-control-for-windows handoff for real upload."

policy:
  allow_implicit_invocation: true
